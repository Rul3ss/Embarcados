Passo 1 - 
            Instale um compilador C, se usar windows, abre o power shell como admin e roda:
                    choco install mingw

compile com o mingw

gcc senoide.c -o senoide.exe

rode o exe com .\senoide.exe

Passo 2 - instalar o python pra rodar o graf.py (opcional)

Importe a biblioteca caso não tenha

pip install matplotlib

e rode ele com:

                    python .\graf.py